// @flow

import { font, colors, colorMap, spacing, typography } from '../../styles';
import { createTheme } from '../functions';

export const BaseTheme = createTheme({
    font,
    colors,
    colorMap,
    spacing,
    typography
});
